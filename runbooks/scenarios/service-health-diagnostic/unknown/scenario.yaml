# Scenario: Service returns unexpected status — unknown
inputs:
  hostname: srv1.example.com

tool_responses:
  "ping:check":
    - exit_code: 0
      stdout: "2 packets transmitted, 2 received"
      outputs:
        result: "2 packets transmitted, 2 received"
  "curl:get":
    - exit_code: 0
      stdout: "502"
      outputs:
        status_code: "502"
        response: "Bad Gateway"

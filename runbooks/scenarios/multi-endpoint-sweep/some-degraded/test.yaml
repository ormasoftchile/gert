# Test: some endpoints degraded
description: One endpoint returns 503 — should escalate
expected_status: completed
expected_outcome: escalated
expected_code: endpoints_degraded
must_reach:
  - check_primary
  - some_unhealthy_end
must_not_reach:
  - all_healthy_end

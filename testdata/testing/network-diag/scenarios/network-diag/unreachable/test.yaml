# DRI selects "unreachable" → branches to network_escalation → escalated
expected_outcome: escalated
expected_captures:
  connectivity_status: "unreachable"
must_reach:
  - ping_target
  - check_http
  - dns_lookup
  - assess_connectivity
  - network_escalation
must_not_reach:
  - confirm_healthy
  - check_proxy_config
  - dns_troubleshoot
description: "Host unreachable — DRI selects unreachable, escalates to networking"
tags:
  - choices
  - branching
  - escalation

# DNS fails → gate.stop_if: escalated → parent stops, never reaches HTTP
description: "DNS gate blocks — child escalates, parent stops before HTTP check"
expected_outcome: escalated
must_reach:
  - dns_gate
must_not_reach:
  - check_http
  - service_healthy
  - http_failed
tags:
  - invoke
  - gate
  - escalation
  - stop_if

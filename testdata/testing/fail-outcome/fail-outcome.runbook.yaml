apiVersion: runbook/v0
meta:
  name: fail-outcome
  vars:
    greeting: hello
steps:
  - id: step_one
    type: cli
    with:
      argv: ["echo", "{{ .greeting }}"]
    capture:
      output: stdout

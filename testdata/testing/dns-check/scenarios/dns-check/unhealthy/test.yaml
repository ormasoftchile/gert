# DNS fails → DRI selects no → outcome escalated
expected_outcome: escalated
expected_captures:
  dns_resolved: "no"
must_reach:
  - resolve_dns
  - check_resolution
  - dns_failed
must_not_reach:
  - dns_healthy
description: "Failed DNS — nonexistent host, DRI confirms failure"
tags:
  - choices
  - branching
  - escalation

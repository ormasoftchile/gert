apiVersion: runbook/v0
meta:
  name: governance-violations
  governance:
    allowed_commands:
      - kubectl
    denied_commands:
      - kubectl
steps:
  - id: step_one
    type: cli
    with:
      argv: ["kubectl", "get", "pods"]
